# Copy to `.env`. Vite only exposes variables prefixed with VITE_.
# Default (/api) uses the dev proxy in vite.config.js. Set to a full URL
# (e.g. https://your-api.onrender.com) when the backend is deployed.
VITE_API_BASE=/api
